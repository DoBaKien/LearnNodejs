<div class="mt-4">
  <div class="row">
    <div class="col-lg-3">
      <button>hoc</button>
    </div>
    <div class="col-lg-9">
      <h2>{{course.name}}</h2>
      <img src="{{course.image}}" class="img-fluid" alt="Responsive image">
      <p>{{course.videoId}}</p>
      <p>{{course.description}}</p>
    </div>
  </div>
</div>